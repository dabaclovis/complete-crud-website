@extends('layouts.app')

@section('content')
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header">
                    <a href="/home" class="d-inline btn btn-secondary text-white">Home</a>
                    <a href="/articles" class="d-inline btn btn-secondary text-white">Articles</a>
                    <a href="/articles/create" class="d-inline btn btn-secondary text-white">Add</a>
                </div>

                <div class="card-body">
                    @if (session('status'))
                        <div class="alert alert-success" role="alert">
                            {{ session('status') }}
                        </div>
                    @endif

                    {{ __('You are logged in!') }}
                </div>
            </div>
        </div>
    </div>
</div>
@endsection
